<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="10dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="10dp"
    android:background="@drawable/cornice_green"
    tools:context="com.progettoids.iotforemergency.gui.HomeActivity">

    <com.progettoids.iotforemergency.gui.MapHome
        android:id="@+id/IMAGEID"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/logout"
        app:srcCompat="@android:drawable/presence_away" />

    <Button
        android:text="Logout"
        android:layout_height="wrap_content"
        android:id="@+id/logout"
        android:textColor="@android:color/background_light"
        android:backgroundTint="#db524c"
        tools:textSize="12sp"
        android:layout_width="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="14dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true" />

    <TextView
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="11dp"
        android:id="@+id/welcome"
        tools:textSize="12sp"
        android:layout_alignBaseline="@+id/logout"
        android:layout_alignBottom="@+id/logout"
        android:layout_toStartOf="@+id/logout" />

</RelativeLayout>
